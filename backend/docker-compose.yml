services:
  backend:
    image: bbogle-backend:latest
    container_name: bbogle-backend
    networks:
      - backend-network
    # restart: unless-stopped
    expose:
      - "8080"

networks:
  backend-network:
    external: true